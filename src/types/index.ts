// Shared cross-feature types accumulate here as each phase introduces
// entities (Transaction, Customer, RiskEvent, CashflowForecast, ...).
// Feature-local types stay inside their own `features/<name>/` folder.
export {};
